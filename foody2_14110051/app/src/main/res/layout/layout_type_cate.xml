<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
        <android.support.v7.widget.RecyclerView
            android:id="@+id/recycleViewTab2"
            android:scrollbars="vertical"
            android:layout_marginBottom="@dimen/btn_cancel_size"
            android:background="@color/theme_light_grey"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
        <!--<ListView
            android:id="@+id/listViewTab2"
            android:layout_marginBottom="@dimen/btn_cancel_size"
            android:background="@color/theme_light_grey"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>-->
        <Button
            android:id="@+id/btnCancelAction2"
            android:layout_width="match_parent"
            android:layout_alignParentBottom="true"
            android:layout_height="@dimen/btn_cancel_size"
            android:background="@color/white"
            android:text="@string/cancel_action"
            android:textAllCaps="false" />
</RelativeLayout>
